# values-staging.yaml
# Updated by Jenkins only when this pipeline is re-run with ENVIRONMENT=staging.

image:
  tag: "d3e337fe-27"
replicaCount: 2

externalSecrets:
  awsSecretName: "staging/currency-converter/api-keys"
  awsSecretProperty: "exchange_rate_api_key"